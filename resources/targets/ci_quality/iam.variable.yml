targets:
  ci_quality:
    variables:
      environment: ${bundle.target}
      # Service principals
      deploy_service_principal_id:
        lookup:
          service_principal: "BIGDATA_CI_QUALITY_CICD"
      run_as_service_principal_id:
        lookup:
          service_principal: "BIGDATA_CI_QUALITY_JOBS_SP"
      # Groups
      group_engenheiros_de_dados:
        description: The name of the Engenheiros de Dados group in Databricks
        default: Data Engineer
      group_analistas_de_dados:
        description: The name of the Analistas de Dados group in Databricks
        default: Data Analytics
      group_cientistas_de_dados:
        description: The name of the Cientistas de Dados group in Databricks
        default: Data Scientist
      group_qualidade_de_dados:
        description: The name of the Qualidade de Dados group in Databricks
        default: Data Quality
      group_data_e_insights:
        description: The name of the Data & Insights group in Databricks
        default: Data & Insights
      group_admin_engenheiros_de_dados:
        description: The name of the Admin Engenheiros group in Databricks
        default: admin_engenheiros_de_dados
      group_brq:
        description: The name of the BRQ group in Databricks
        default: Big Data BRQ

      # Agrupamento de permissões

      raw_uc_grants:
        description: The grants to apply to raw catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - BROWSE
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA

      brz_uc_grants:
        description: The grants to apply to brz catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - BROWSE
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA
      svo_uc_grants:
        description: The grants to apply to svo catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA

      slv_uc_grants:
        description: The grants to apply to slv catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA
      gld_uc_grants:
        description: The grants to apply to gld catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA
      bic_uc_grants:
        description: The grants to apply to bic catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - BROWSE
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA
      ext_uc_grants:
        description: The grants to apply to ext catalog
        type: complex
        default:
          - principal: "${var.group_engenheiros_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_cientistas_de_dados}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_brq}"
            privileges:
              - MANAGE
              - ALL_PRIVILEGES
          - principal: "${var.group_analistas_de_dados}"
            privileges:
              - BROWSE
          - principal: "${var.run_as_service_principal_id}"
            privileges:
              - BROWSE
              - USE_CATALOG
              - USE_SCHEMA
              - SELECT
              - EXECUTE
              - READ_VOLUME
              - MODIFY
              - REFRESH
              - WRITE_VOLUME
              - CREATE_TABLE
              - CREATE_SCHEMA
          
        

